# Day 47 - Array Slicing and Finding the Index Value

## Overview

This practice focuses on extracting a portion of an array and finding the index of a specific value using JavaScript array methods.

## Features

* Create and work with arrays
* Extract elements using `slice()`
* Find the index of a value using `indexOf()`
* Display the selected array values
* Practice array searching and manipulation

## Technologies Used

* JavaScript

## Concepts Practiced

* Arrays
* `slice()`
* `indexOf()`
* Array indexing
* Array searching
* Array manipulation
* Console output

## Learning Outcome

By completing this practice, I learned how to:

* Use `slice()` to extract a portion of an array without modifying the original array.
* Use `indexOf()` to find the position of a value.
* Combine array methods to retrieve and work with specific data.

## Use Case

Array slicing and indexing are useful when working with selected portions of data, searching lists, processing collections, and displaying specific items.

## Commit Message

```text
Day 47 - Practiced slice() and indexOf() Array Methods
```

## GitHub Description

```text
Day 47 JavaScript Practice - Array Slicing with slice() and Finding Index Values using indexOf().
```

### Day 47 Achievement

Completed **47 days of JavaScript practice**, improving my understanding of array slicing, indexing, and searching methods.
