# Day 49 - Splice Playground

## Overview

This practice focuses on using the JavaScript `splice()` method to add, remove, and modify elements in an array.

## Features

* Create and work with arrays
* Remove elements using `splice()`
* Add new elements at a specific position
* Replace existing array elements
* Display the updated array
* Practice array manipulation

## Technologies Used

* JavaScript

## Concepts Practiced

* Arrays
* `splice()`
* Adding array elements
* Removing array elements
* Replacing array elements
* Array indexing
* Console output

## Learning Outcome

By completing this practice, I learned how to:

* Use `splice()` to remove elements from an array.
* Insert new values at a specific index.
* Replace existing values in an array.
* Modify arrays dynamically using JavaScript.

## Use Case

The `splice()` method is useful for managing dynamic lists, updating collections, removing items, and inserting new data at specific positions.

## Commit Message

```text
Day 49 - Practiced splice() Array Method
```

## GitHub Description

```text
Day 49 JavaScript Practice - Using splice() to Add, Remove, and Modify Array Elements.
```

### Day 49 Achievement

Completed **49 days of JavaScript practice**, strengthening my understanding of JavaScript array manipulation methods.
